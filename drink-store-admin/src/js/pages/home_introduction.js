// src/js/pages/home_introduction.js
document.addEventListener('DOMContentLoaded', function() {
    console.log('Home Introduction page specific JavaScript loaded!');

    // Hiện tại trang này không có nhiều logic JavaScript phức tạp.
    // Bạn có thể thêm code vào đây nếu sau này trang "Trang Chủ" có các yếu tố tương tác đặc biệt.
    // Ví dụ: hiệu ứng animation cho các info-card, tải động nội dung, v.v.
});